{
	"$schema": "https://www.raycast.com/schemas/extension.json",
	"name": "clipboard-html-to-markdown",
	"title": "Clipboard Html to Markdown",
	"description": "Converts Html contents of Clipboard to markdown",
	"icon": "extension-icon.png",
	"author": "jonathan_coleman",
	"owner": "oncarrot",
	"license": "MIT",
	"commands": [
		{
			"name": "clipboard-html-to-markdown",
			"title": "Clipboard Html to Markdown",
			"description": "Converts HTML contents of Clipboard to markdown and adds it to the clipboard",
			"mode": "no-view"
		},
		{
			"name": "clipboard-html-to-markdown-paste",
			"title": "Paste Clipboard Html to Markdown",
			"description": "Converts HTML contents of Clipboard to markdown and pastes it",
			"mode": "no-view"
		}
	],
	"dependencies": {
		"@raycast/api": "^1.97.0",
		"@raycast/utils": "^1.17.0",
		"turndown": "^7.2.0"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@raycast/eslint-config": "^2.0.4",
		"@types/node": "22.13.10",
		"@types/react": "19.0.10",
		"@types/turndown": "^5.0.5",
		"eslint": "^9.22.0",
		"prettier": "^3.5.3",
		"typescript": "^5.8.2"
	},
	"scripts": {
		"build": "ray build",
		"dev": "ray develop",
		"fix-lint": "ray lint --fix",
		"lint": "ray lint",
		"prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
		"publish": "npx @raycast/api@latest publish"
	}
}
